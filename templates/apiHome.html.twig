{% extends "base.html.twig" %}

{% block title %}API{% endblock %}



{% block body %}
                <h1>API</h1>
                <div class="info">
                    <p>Länkar till routerna:</p>

                    <li><a href="{{ path('apiHome') }}">API (denna routen)</a></li>
                    <li><a href="{{ path('quote') }}">Quote routen</a></li>
                    <li><a href="{{ path('numbers') }}">Lucky numbers routen</a></li>
                </div>

                <div class="info">
                    <p>Länkar för kmom02:</p>

                    <li><a href="{{ path('api_deck') }}">API deck, draw <strong>all</strong> cards</a></li>
                    <li><a href="{{ path('api_deck_shuffle') }}">API deck, draw <strong>all shuffled</strong> cards</a></li>
                    {# <li><a href="{{ path('api_deck_draw_one_get') }}">API deck, draw <strong>one</strong> card</a></li> #}
                    <li><a href="{{ path('test_draw_num_cards_api_get', {num: 4}) }}">API deck, draw <strong>multiple</strong> cards</a></li>


                    <li><a href="{{ path('api_deck_draw_one_get') }}">Draw <strong>one</strong> card</a></li>
                </div>
{% endblock %}
